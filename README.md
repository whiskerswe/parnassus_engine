# parnassus_engine

Python project using a `src/` layout. This is an experimental playground to learn more about music as data.

## Setup

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
python -m pip install -e .
```

For development and tests, install the dev requirements instead:

```powershell
python -m pip install -r requirements-dev.txt
```

## Run

```powershell
python -m parnassus_engine
```

## Test

```powershell
python -m pytest
```

## Test resources

Edvard Grieg Lyrical Pieces Op 12 no 3 Wächterlied
Leipzig: C.F. Peters, No. 1269, n.d.(ca.1895) (Public domain)
https://imslp.org/wiki/Lyric_Pieces,_Op.12_(Grieg,_Edvard)
https://imslp.org/wiki/Special:ReverseLookup/336077
